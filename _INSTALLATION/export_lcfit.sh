#!/bin/sh

#This script prepares a tar bundle which can be downloaded from the
#lcfit website and installed.  The tar bundle will be based on the
#current installation.


# dont dump the database, since have a base set of definitions already in the install directory

# export the code using svn.  i think that is all ...

# put it on the website?